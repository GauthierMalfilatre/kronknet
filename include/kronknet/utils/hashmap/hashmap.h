/*
** FREE PROJECT, 2026
** KRONKNET
** File description:
** Header file for KRONKNET, a simple ring-buffer library in C.
*/
#ifndef KRONKNET_HASHMAP_H
    #define KRONKNET_HASHMAP_H
    #include "kronknet/macros/optimization.h"
#include <stddef.h>
#include <stdint.h>

///////////////////////////////////////////////////////////////////////////////
/**
 * @brief  Forward declaration of knMap type
 */
///////////////////////////////////////////////////////////////////////////////
typedef struct kronknet_hashmap_s knMap;
///////////////////////////////////////////////////////////////////////////////


///////////////////////////////////////////////////////////////////////////////
/**
 * @brief  Define function ptr type to hash
 */
///////////////////////////////////////////////////////////////////////////////
typedef uint64_t (*knMapHash)(uint64_t);
///////////////////////////////////////////////////////////////////////////////


///////////////////////////////////////////////////////////////////////////////
/**
 * @brief Basic hashing (thomas Wang) of uint64_t
 *
 * @param key  The key to hash
 * @return     Returns the hashed key
 */
///////////////////////////////////////////////////////////////////////////////
KN_API uint64_t knMap_basicHash(uint64_t key);
///////////////////////////////////////////////////////////////////////////////


///////////////////////////////////////////////////////////////////////////////
/**
 * @brief  Create a new knMap*
 *
 * @param hashFunc  the function ptr to hash keys with
 * @param length    the length of the hashtable
 * @return          the newly created knMap*, or NULL if error occurs
 */
///////////////////////////////////////////////////////////////////////////////
KN_API knMap* knMap_create(knMapHash hashFunc, size_t length);
///////////////////////////////////////////////////////////////////////////////


///////////////////////////////////////////////////////////////////////////////
/**
 * @brief  Destroy a knMap
 *
 * @param map  The map to destroy
 */
///////////////////////////////////////////////////////////////////////////////
KN_API void knMap_destroy(knMap* map);
///////////////////////////////////////////////////////////////////////////////


///////////////////////////////////////////////////////////////////////////////
/**
 * @brief  Insert data into the knMap*
 *
 * @param map    The map to insert value
 * @param key    The key to hash
 * @param value  The value to insert
 * @return       -1 on error, 0 otherwise
 */
///////////////////////////////////////////////////////////////////////////////
KN_API int knMap_insert(knMap* map, uint64_t key, void *value);
///////////////////////////////////////////////////////////////////////////////


///////////////////////////////////////////////////////////////////////////////
/**
 * @brief Delete data from the knMap
 *
 * @param map  The map into delete
 * @param key  The key of the data to delete
 * @return     -1 on error, 0 otherwise
 */
///////////////////////////////////////////////////////////////////////////////
KN_API int knMap_delete(knMap* map, uint64_t key);
///////////////////////////////////////////////////////////////////////////////


///////////////////////////////////////////////////////////////////////////////
/**
 * @brief  Search data into the knMap
 *
 * @param map  The map to inspect
 * @param key  The key of the data to search
 * @return     the void* value if found, NULL otherwise
 */
///////////////////////////////////////////////////////////////////////////////
KN_API void* knMap_search(const knMap* map, uint64_t key);
///////////////////////////////////////////////////////////////////////////////

#endif /* KRONKNET_HASHMAP_H */
